<% if user_signed_in? %>
  <%= link_to "New Article", new_article_path, class: "btn btn-default btn-lg", id: "new-article-button" %>
<% end %>
<% if @articles.any? %>
  <%= render @articles %>
<% else %>
  <h1 id="no-articles">No Articles Created</h1>
<% end %>
